# Desktop App — Environment Variables
# KUBECONFIG path (leave blank to use ~/.kube/config auto-detection)
KUBECONFIG=

# Kubernetes namespace to monitor (empty = all namespaces)
K8S_NAMESPACE=default

# Refresh interval for live panels (seconds)
REFRESH_INTERVAL_SECONDS=15

LOG_LEVEL=INFO
